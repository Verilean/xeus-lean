{
  "name": "xeus-lean-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "End-to-end tests for xeus-lean JupyterLite deployment",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed",
    "test:update": "UPDATE_SNAPSHOTS=1 playwright test",
    "install-browsers": "playwright install chromium"
  },
  "devDependencies": {
    "@playwright/test": "1.56.0",
    "@types/node": "^20.0.0",
    "typescript": "^5.4.0"
  }
}
